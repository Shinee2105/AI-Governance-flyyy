# =============================================================================
# Flyyy.ai backend - environment configuration
# Copy this file to `.env` and fill in the placeholders.
# NEVER commit your real .env (it is git-ignored).
# =============================================================================

# ---- Core ----
ENVIRONMENT=development
APP_NAME=Flyyy.ai - SaaS AI Governance

# ---- Database ----
# Default: local SQLite file (works out-of-the-box, no setup).
DATABASE_URL=sqlite:///./flyyy.db
# Production example (PostgreSQL):
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/flyyy

# ---- Security (REPLACE before any real deployment) ----
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_admin_2025
SECRET_KEY=dev-secret-key-please-change-in-production
TOKEN_EXPIRE_MINUTES=1440

# ---- CORS ----
# Comma-separated list of allowed frontend origins. In development this is the
# Vite dev server. In production set it to your deployed frontend origin.
# Never use "*" together with credentials.
BACKEND_CORS_ORIGINS=http://localhost:5173

# ---- Demo data ----
# When true, the app seeds realistic SIMULATED data so it runs without SaaS creds.
SEED_DEMO_DATA=true

# ---- Salesforce connector (OPTIONAL - fill to discover REAL evidence) ----
# 1. Enable External Client Apps in your Salesforce org.
# 2. Create an External Client App (Connected App) with OAuth 2.0 Client
#    Credentials grant type and scopes: api, refresh_token, openid.
# 3. Set a Run-As integration user and paste the credentials below, then set
#    SFDC_ENABLED=true.
# 4. (Monitoring) For Agentforce session traces, get session IDs from the
#    Session Trace UI and set SFDC_OTEL_SESSION_IDS=comma,separated,ids.
SFDC_ENABLED=false
SFDC_DOMAIN=YOUR_ORG_DOMAIN.my.salesforce.com
SFDC_CLIENT_ID=YOUR_CONNECTED_APP_CONSUMER_KEY
SFDC_CLIENT_SECRET=YOUR_CONNECTED_APP_CONSUMER_SECRET
SFDC_API_VERSION=62.0
SFDC_OTEL_SESSION_IDS=
